/* OpenJax TUI Styles */

/* 全局颜色定义 */
$primary: #00BFFF;
$secondary: #808080;
$success: #00FF00;
$background: #1a1a1a;
$surface: #2d2d2d;
$text: #ffffff;
$border: #3d3d3d;

/* 屏幕样式 */
Screen {
    align: center middle;
    background: $background;
}

/* Header 样式 */
Header {
    background: $surface;
    color: $primary;
    height: 3;
    text-style: bold;
}

Header.-tall {
    height: 3;
}

HeaderTitle {
    color: $primary;
    text-style: bold;
}

HeaderIcon {
    color: $primary;
}

/* Footer 样式 */
Footer {
    background: $surface;
    color: $secondary;
    height: 1;
}

FooterKey {
    color: $primary;
    background: $surface;
}

FooterKeyDescription {
    color: $text;
    background: $surface;
}

/* 聊天容器 */
#chat-container {
    width: 100%;
    height: 100%;
    padding: 1 2;
}

/* 聊天日志区域 */
#chat-log {
    width: 100%;
    height: 1fr;
    border: solid $primary;
    background: $surface;
    padding: 1;
    color: $text;
}

/* 输入框 */
#chat-input {
    width: 100%;
    margin-top: 1;
    background: $surface;
    color: $text;
    border: solid $border;
}

#chat-input:focus {
    border: solid $primary;
}

/* 滚动条样式 */
Scrollbar {
    background: $surface;
    color: $primary;
}

ScrollbarCorner {
    background: $surface;
}
