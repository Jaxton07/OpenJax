# Backward Compatibility Plan

## 兼容目标
- 老会话、老事件、不完整 payload 不得导致页面崩溃。

## 策略
- 无结构化字段时自动降级纯文本渲染。
- 未知事件类型记录日志并跳过，不中断会话流。
- 字段缺失时使用安全默认值（例如 `title=tool`）。

## 验证清单
- 旧会话历史加载可读。
- 新旧消息混排无布局错误。
- reducer 遇到未知事件不抛异常。
