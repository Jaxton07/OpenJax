# Skills Roadmap

## 下一阶段

1. `user-invocable` 显式触发
2. `requires` 依赖检查与提示
3. `disable-model-invocation` 语义支持
4. 文件系统 watcher 热更新
5. 子代理模型/工具约束

## 兼容深化目标

1. 对齐更多 Claude/OpenClaw 字段语义
2. 降低 prompt 注入体积并保持匹配质量
